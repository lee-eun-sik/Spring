<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="back.mapper.pet.PetMapper">

    <insert id="insertPet" parameterType="back.model.Pet.Pet">
        INSERT INTO pet (
            user_id, animal_name, animal_species, animal_adoption_date,
            birth_date, gender, animal_memo, image_path, create_id, update_id, del_yn
        )
        VALUES (
            #{userId}, #{animalName}, #{animalSpecies}, #{animalAdoptionDate},
            #{birthDate}, #{gender}, #{animalMemo}, #{imagePath},
            #{createId}, #{updateId}, #{delYn}
        )
    </insert>

</mapper>